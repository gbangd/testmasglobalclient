Test MasGlobal Client

Instructions

Update the environment variable urlWebAPI with the corresponding value of testing web server url.

Sample configuration: 

export const environment = {
  //....Other variables
  urlWebAPI: "http://localhost:51120/api"
};
